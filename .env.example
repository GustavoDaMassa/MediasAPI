DATABASE_PASSWORD=your_database_password
DATABASE_ROOT_PASSWORD=your_root_password
SPRING_PROFILES_ACTIVE=prod
CORS_ALLOWED_ORIGINS=https://mediasapi.gustavohdev.com.br

# JWT keys: single line, no PEM headers, no newlines
# Generate with: cat key.pem | sed '/^-----/d' | tr -d '\n'
JWT_PUBLIC_KEY_CONTENT=your_rsa_public_key_base64
JWT_PRIVATE_KEY_CONTENT=your_rsa_private_key_base64
